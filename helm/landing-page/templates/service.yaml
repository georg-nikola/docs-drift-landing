apiVersion: v1
kind: Service
metadata:
  name: {{ include "docs-drift-landing.fullname" . }}
  labels:
    {{- include "docs-drift-landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docs-drift-landing.selectorLabels" . | nindent 4 }}
